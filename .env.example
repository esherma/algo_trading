# ============================================================
#  BTM Trading — Environment Variables Template
#  Copy this file to .env and fill in your values.
#  NEVER commit .env to version control.
# ============================================================

# ── Alpaca paper-trading credentials ─────────────────────────
ALPACA_PAPER_API_KEY=your_paper_key_here
ALPACA_PAPER_API_SECRET=your_paper_secret_here

# ── Alpaca live-trading credentials ──────────────────────────
#    Only required if using --session live
ALPACA_LIVE_API_KEY=your_live_key_here
ALPACA_LIVE_API_SECRET=your_live_secret_here

# ── Email (SMTP) ─────────────────────────────────────────────
#    The morning bands chart is sent via standard SMTP.
#
#    Gmail example:
#      SMTP_SERVER=smtp.gmail.com
#      SMTP_PORT=587
#      SMTP_USERNAME=you@gmail.com
#      SMTP_PASSWORD=xxxx-xxxx-xxxx-xxxx   ← 16-char app password
#                                             (requires Gmail 2FA enabled)
#
#    Outlook/Office 365:
#      SMTP_SERVER=smtp.office365.com
#      SMTP_PORT=587
#
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=sender@example.com
SMTP_PASSWORD=your_smtp_password_here

#    Where to deliver the morning email
RECIPIENT_EMAIL=your_email@example.com
